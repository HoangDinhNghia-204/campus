# ------------------------------------------------------------------------------
# IGNORE EVERYTHING IN THE ROOT DIRECTORY BY DEFAULT
# ------------------------------------------------------------------------------
# This ignores all files and directories in the project root.
# We will then explicitly un-ignore the files and directories we want to track.
*

# ------------------------------------------------------------------------------
# DO NOT IGNORE THESE SPECIFIC FILES AND DIRECTORIES
# ------------------------------------------------------------------------------
# The exclamation mark (!) means "do not ignore".

# Un-ignore the .gitignore file itself so it gets committed.
!/.gitignore

# Keep the main composer file.
!/composer.json

# If you have a composer.lock file, it's highly recommended to keep it.
# This ensures everyone uses the same dependency versions.
# Remove the line below if you don't want to track it.
!/composer.lock

# Keep your custom modules directory.
# The trailing slash is important to specify it's a directory.
!/web/modules/custom/

# Keep your custom themes directory.
!/web/themes/custom/

# ------------------------------------------------------------------------------
# IGNORE SPECIFIC FILES WITHIN THE ALLOWED DIRECTORIES
# ------------------------------------------------------------------------------
# These are files often generated by IDEs or operating systems.

# Ignore macOS system files.
.DS_Store

# Ignore IDE configuration files.
/.idea/
/.vscode/

# Ignore compiled Sass files if you're using a preprocessor.
# Example: if you have .css files inside your custom theme that are generated from .scss
# web/themes/custom/**/*.css
# web/themes/custom/**/.sass-cache/
